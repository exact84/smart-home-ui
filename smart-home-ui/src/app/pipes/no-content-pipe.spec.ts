import { NoContentPipe } from './no-content-pipe';

describe('NoContentPipe', () => {
  it('create an instance', () => {
    const pipe = new NoContentPipe();
    expect(pipe).toBeTruthy();
  });
});
